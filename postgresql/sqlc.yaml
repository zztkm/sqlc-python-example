version: 2
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      python:
        package: "db"
        out: "db"
        emit_sync_querier: true
        emit_pydantic_models: true
